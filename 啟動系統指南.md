# 系統啟動詳細指南

## 第一步：設置 Supabase（如果您還沒有）

### 1.1 註冊 Supabase 帳號
1. 開啟瀏覽器，前往 https://supabase.com
2. 點擊「Start your project」或「Sign Up」
3. 使用 Google 帳號或 Email 註冊（免費）
4. 完成註冊後登入

### 1.2 建立新專案
1. 點擊「New Project」按鈕
2. 填寫專案資訊：
   - **Name**: 輸入專案名稱（例如：mystudio-booking）
   - **Database Password**: 設定資料庫密碼（請記住這個密碼）
   - **Region**: 選擇 Southeast Asia (Singapore) 或離您最近的區域
3. 點擊「Create new project」
4. 等待約 2-3 分鐘讓系統設置完成

### 1.3 獲取 API 金鑰
1. 專案建立完成後，在左側選單點擊「Settings」→「API」
2. 在「Project API keys」區域找到：
   - **Project URL**: 看起來像 `https://abcdefg.supabase.co`
   - **anon public key**: 看起來像 `eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...`
3. **重要**：複製這兩個值（等下會用到）

### 1.4 執行資料庫 Schema
1. 在左側選單點擊「SQL Editor」
2. 點擊「New query」
3. 開啟專案中的 `supabase-schema.sql` 檔案
4. 將整個 SQL 內容複製貼上到 SQL Editor 中
5. 點擊「Run」按鈕執行
6. 等待執行完成（應該會看到「Success」訊息）

## 第二步：設置環境變數

### 2.1 建立 .env 檔案
在專案根目錄（有 package.json 的地方）執行：

**Windows PowerShell:**
```powershell
Copy-Item .env.example .env
```

**或手動複製：**
1. 開啟 `.env.example` 檔案
2. 另存新檔為 `.env`（注意檔名前面有點）

### 2.2 填入正確的 API 資訊
開啟 `.env` 檔案，將以下內容替換：

```env
# 將 your_supabase_project_url 替換為您的 Project URL
# 正確格式：https://your-project-id.supabase.co
# 錯誤格式：https://your-project-id.supabase.co/rest/v1/ (不要加 /rest/v1/)
VITE_SUPABASE_URL=https://gitcjksnelwxdwjgoeeo.supabase.co

# 將 your_supabase_anon_key 替換為您的 anon public key
# 正確格式：eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
# 錯誤格式：sb_secret_... (不要用 sb_secret_ 開頭的)
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# 租戶 ID 保持不變
VITE_TENANT_ID=default-tenant
```

**⚠️ 重要提醒：**
- URL 不要包含 `/rest/v1/`
- Anon key 應該以 `eyJ` 開頭，不是 `sb_secret_`
- 如果您填入的格式錯誤，系統無法正常運作

## 第三步：安裝依賴套件

在專案根目錄開啟終端機，執行：

```bash
npm install
```

如果已經安裝過，可以跳過這步。

## 第四步：啟動系統

在專案根目錄執行：

```bash
npm run dev
```

您會看到類似這樣的輸出：
```
  VITE v5.x.x  ready in xxx ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: use --host to expose
```

## 第五步：測試系統

1. 開啟瀏覽器，訪問 http://localhost:5173
2. 您應該能看到AJ創美學苑的首頁
3. 測試預約功能：
   - 點擊「線上預約」
   - 選擇服務 → 選擇老師 → 選擇日期 → 選擇時段
   - 填寫個人資訊並提交
4. 測試查詢預約：
   - 點擊「我的預約」
   - 輸入剛才填寫的手機號碼
   - 應該能看到剛才的預約紀錄
5. 測試工作室設定：
   - 點擊「設定」
   - 修改工作室名稱或顏色
   - 點擊「儲存設定」

## 常見問題解決

### 問題 1：系統無法連接 Supabase
**症狀：** 預約時出現錯誤，或查詢不到資料

**解決方法：**
1. 檢查 `.env` 檔案中的 URL 和 key 是否正確
2. 確認 URL 格式為 `https://xxx.supabase.co`（沒有 `/rest/v1/`）
3. 確認 anon key 以 `eyJ` 開頭
4. 重新啟動系統：`Ctrl+C` 停止，然後 `npm run dev`

### 問題 2：資料庫 Schema 未執行
**症狀：** 查詢時出現「relation does not exist」錯誤

**解決方法：**
1. 回到 Supabase Dashboard
2. 進入 SQL Editor
3. 重新執行 `supabase-schema.sql` 的內容
4. 確認執行成功

### 問題 3：npm install 失敗
**症狀：** 安裝套件時出現錯誤

**解決方法：**
```bash
# 清除快取後重新安裝
npm cache clean --force
npm install
```

### 問題 4：端口被佔用
**症狀：** 啟動時顯示「Address already in use」

**解決方法：**
```bash
# 使用不同端口啟動
npm run dev -- --port 3000
```

## 檢查清單

啟動前確認：
- [ ] 已註冊 Supabase 帳號
- [ ] 已建立 Supabase 專案
- [ ] 已執行資料庫 Schema
- [ ] 已建立 `.env` 檔案
- [ ] `.env` 中的 URL 格式正確（沒有 `/rest/v1/`）
- [ ] `.env` 中的 anon key 格式正確（以 `eyJ` 開頭）
- [ ] 已執行 `npm install`
- [ ] 已執行 `npm run dev`
- [ ] 瀏覽器能訪問 http://localhost:5173

## 需要協助？

如果遇到問題：
1. 檢查瀏覽器控制台（F12）的錯誤訊息
2. 確認 Supabase 專案狀態是否正常
3. 重新檢查環境變數設定
4. 查看終端機的錯誤輸出

## 快速開始（如果您已經有 Supabase）

如果您已經設置好 Supabase，只需要：

```bash
# 1. 建立 .env 檔案並填入正確的 API 資訊
Copy-Item .env.example .env

# 2. 編輯 .env 檔案，填入您的 Supabase URL 和 anon key

# 3. 啟動系統
npm run dev

# 4. 開啟瀏覽器訪問 http://localhost:5173
```

祝您使用順利！
